{# -*- mode: web; web-mode-markup-indent-offset: 2 -*-  -->  #}
{% set pagetitle = "selectprovider" %}
{% extends "base.twig" %}

{% block scripts %}
  <script>
   var acrequest = {{requestJSON|raw}};
   window.activeAccounts = {{activeAccountsJSON|raw}};
   window.loc = {{locationJSON|raw}};
   window.config = {{configuration|raw}};
   window.dictionary = {{dictionary|raw}};
   window.client = {{client|raw}};
  </script>

  <script>
   require(['accountchooser/main']);
  </script>

{% endblock scripts %}



{% block content %}

  <div class="pure-g">
    <div class="pure-u-1">

      <div class="main box">
        {% include 'partials/accounts.twig' %}
        {% include 'partials/disco.twig' %}
      </div>
    </div>

  </div>
{% endblock %}
